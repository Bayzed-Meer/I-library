const baseUrl = 'https://librify-services.onrender.com/api/v1';

export const environment = {
  apiUrl: `${baseUrl}`,
  auth_url: `${baseUrl}/auth`,
  users_url: `${baseUrl}/users`,
  books_url: `${baseUrl}/books`,
  health_url: `${baseUrl}/health-check`,
};
